// Analysis provider setup.
// @xenova/transformers is not compatible with the Metro bundler (uses import.meta).
// For MVP, the app falls back to manual mood selection when no provider is registered.
// Post-MVP: register a native module provider here.
